<%= render 'admin/admin_menu' %>

<%= render @region %>

<div>
  <%= link_to "Edit this region", edit_admin_region_path(@region) %> |
  <%= link_to "Back to regions", admin_regions_path %>

  <%= button_to "Destroy this region", admin_region_path(@region), method: :delete %>
</div>
