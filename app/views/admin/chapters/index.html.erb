<%= render 'admin/admin_menu' %>

<h1>Chapters</h1>

<div id="chapters">
  <% @chapters.each do |chapter| %>
    <%= render chapter %>
    <p>
      <%= link_to "Show this chapter", admin_chapter_path(chapter) %>
    </p>
  <% end %>
</div>

<%= link_to "New chapter", new_admin_chapter_path %>
